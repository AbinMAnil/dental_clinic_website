
  # dental cliniic (Community)

  This is a code bundle for dental cliniic (Community). The original project is available at https://www.figma.com/design/IcuUpzVKNtRozWz5dlCRaR/dental-cliniic--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  